name: Twitter 24/7 Ghost & Bot Purger (Halted / Manual Only)

on:
  workflow_dispatch: # Manual only - no automatic schedules

concurrency:
  group: twitter-ghost-purger
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  run-unfollower:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow is halted. Manual triggers only."
